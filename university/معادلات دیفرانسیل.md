***

# دسته‌بندی کلی معادلات دیفرانسیل

معادلات دیفرانسیل، معادلاتی هستند که در آن‌ها مشتق توابع نسبت به یک یا چند متغیر ظاهر می‌شود. این معادلات به طور کلی به دسته‌های زیر تقسیم می‌شوند:

## 1. معادلات دیفرانسیل معمولی (ODE)

در این نوع معادلات، مشتقات فقط نسبت به یک متغیر مستقل ظاهر می‌شوند. تابع مجهول فقط به یک متغیر وابسته است.

مثال:
$$
\frac{dy}{dx} = y
$$

## 2. معادلات دیفرانسیل جزئی (PDE)

در این نوع معادلات، مشتقات جزئی نسبت به دو یا چند متغیر مستقل ظاهر می‌شوند. تابع مجهول وابسته به چند متغیر است.

مثال:
$$
\frac{\partial u}{\partial t} = k \frac{\partial^2 u}{\partial x^2}
$$

## 3. مرتبه معادله

مرتبه معادله، بالاترین مرتبه مشتق ظاهر شده در آن است. مثلاً اگر مشتق دوم وجود داشته باشد اما سوم وجود نداشته باشد، معادله مرتبه دوم است.

مثال معادله مرتبه دوم:
$$
y'' + y = 0
$$

## 4. معادلات خطی و غیرخطی

اگر تابع مجهول و مشتق‌های آن به توان یک ظاهر شوند و ضربی از آن‌ها وجود نداشته باشد، معادله **خطی** است. در غیر این صورت، معادله **غیرخطی** محسوب می‌شود.

**خطی:**
$$
y'' - 3y' + 2y = 0
$$

**غیرخطی:**
$$
y' = y^2 + x
$$

***
***

# دسته‌بندی انواع معادلات دیفرانسیل درجه یک

در این قسمت، معادلات دیفرانسیل مرتبه اول به چند دسته مهم تقسیم می‌شوند. برای هر دسته: روش تشخیص، روش حل، نکات کلیدی و تمرین‌های متنوع حل‌شده آمده‌اند.

 

## 1. معادلات قابل تفکیک (Separable Equations)

### 🔍 روش تشخیص

اگر معادله به فرم $\frac{dy}{dx} = g(x)\cdot h(y)$ باشد و بتوان $x$ و $y$ را از هم جدا کرد، معادله قابل تفکیک است.

### 🛠️ روش حل

$\frac{1}{h(y)}dy = g(x)dx$  
سپس از طرفین انتگرال می‌گیریم.

### 💡 نکات

- همیشه بعد از انتگرال‌گیری ثابت انتگرال را فراموش نکنید.
    
- برخی معادلات قابل تفکیک، بعد از بازنویسی به این فرم درمی‌آیند.
    

 

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - تابع مثلثاتی و نمایی):

حل کنید:

$$dydx=xcos⁡y1+x2\frac{dy}{dx} = \frac{x \cos y}{1 + x^2}$$

**حل:**  
$\frac{1}{\cos y}dy = \frac{x}{1 + x^2}dx$  
$\Rightarrow \int \sec y,dy = \int \frac{x}{1 + x^2}dx$  
$\Rightarrow \ln|\sec y + \tan y| = \frac{1}{2}\ln(1 + x^2) + C$

 

#### تمرین 2 (دشوار - رادیکالی):

حل کنید:

$$dydx=1−y21+x2\frac{dy}{dx} = \frac{\sqrt{1 - y^2}}{1 + x^2}$$

**حل:**  
$\frac{dy}{\sqrt{1 - y^2}} = \frac{dx}{1 + x^2}$  
$\Rightarrow \arcsin y = \arctan x + C$  
$\Rightarrow y = \sin(\arctan x + C)$

 

## 2. معادلات همگن (Homogeneous)

### 🔍 روش تشخیص

اگر $\frac{dy}{dx} = f\left(\frac{y}{x}\right)$، آنگاه معادله همگن است.

### 🛠️ روش حل

تغییر متغیر $y = vx \Rightarrow dy = v dx + x dv$  
جای‌گذاری در معادله و سپس تفکیک متغیرها و حل.

### 💡 نکات

- لازم است تابع سمت راست فقط تابعی از $\frac{y}{x}$ باشد.
    
- اگر تابع به سادگی قابل تشخیص نیست، امتحان کنید آیا $f(tx, ty) = f(x, y)$ برقرار است.
    

 

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - رادیکال در نسبت):

حل کنید:

$$dydx=1+(yx)2\frac{dy}{dx} = \sqrt{1 + \left(\frac{y}{x}\right)^2}$$

**حل:**  
$y = vx \Rightarrow dy = v dx + x dv$  
$\Rightarrow v + x\frac{dv}{dx} = \sqrt{1 + v^2} \Rightarrow \frac{x dv}{\sqrt{1 + v^2} - v} = dx$  
با روش جزء‌به‌جزء یا جایگذاری حل می‌شود.

 

#### تمرین 2 (دشوار - تابع مرکب):

حل کنید:

$$dydx=x2+xy+y2x2\frac{dy}{dx} = \frac{x^2 + xy + y^2}{x^2}$$

**حل:**  
تقسیم صورت و مخرج بر $x^2$:  
$\frac{dy}{dx} = 1 + \frac{y}{x} + \left(\frac{y}{x}\right)^2$  
اکنون $\frac{dy}{dx} = f(y/x)$  
$y = vx \Rightarrow dy = v dx + x dv$  
در ادامه تفکیک و حل می‌شود.

 

## 3. معادلات خطی (Linear First-Order)

### 🔍 روش تشخیص

شکل کلی: $\frac{dy}{dx} + P(x)y = Q(x)$

### 🛠️ روش حل

1. محاسبه عامل فاکتور انتگرال:  
    $\mu(x) = e^{\int P(x)dx}$
    
2. ضرب معادله در $\mu(x)$
    
3. حل با استفاده از مشتق حاصل‌ضرب
    

### 💡 نکات

- حتی اگر تابع $P(x)$ پیچیده باشد، فاکتور انتگرال مهم‌ترین مرحله است.
    

 

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - تابع لگاریتمی):

حل کنید:

$$dydx+yln⁡x=x\frac{dy}{dx} + y \ln x = x$$

**حل:**  
$\mu(x) = e^{\int \ln x dx} = e^{x\ln x - x}$  
ضرب در عامل فاکتور و ادامه حل...

 

#### تمرین 2 (دشوار - تابع نمایی و تانژانت):

حل کنید:

$$dydx+yex=tan⁡x\frac{dy}{dx} + y e^x = \tan x$$

**حل:**  
$\mu(x) = e^{\int e^x dx} = e^{e^x}$  
سپس ضرب، مشتق حاصل‌ضرب، و انتگرال سمت راست.

 

## 4. معادلات دقیق (Exact Equations)

### 🔍 روش تشخیص

اگر معادله به فرم $M(x,y)dx + N(x,y)dy = 0$ باشد و شرط $\frac{\partial M}{\partial y} = \frac{\partial N}{\partial x}$ برقرار باشد، معادله دقیق است.

### 🛠️ روش حل

تابع پتانسیل $\psi(x,y)$ را بیابید به‌طوری‌که $\frac{\partial \psi}{\partial x} = M$ و $\frac{\partial \psi}{\partial y} = N$  
سپس $\psi(x,y) = C$

### 💡 نکات

- همیشه شرط دقت را بررسی کنید.
    
- اگر شرط برقرار نیست، به فکر روش integrating factor باشید.
    

 

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - چندجمله‌ای درجه دوم):

حل کنید:

$$(2xy+y2)dx+(x2+2xy)dy=0(2xy + y^2)dx + (x^2 + 2xy)dy = 0$$

**حل:**  
$M = 2xy + y^2,\ N = x^2 + 2xy$  
$\frac{\partial M}{\partial y} = 2x + 2y,\ \frac{\partial N}{\partial x} = 2x + 2y$  
پس دقیق است.

$\psi(x,y) = \int M dx = x^2 y + x y^2 + h(y)$  
$\Rightarrow \frac{\partial \psi}{\partial y} = x^2 + 2xy + h'(y)$  
$\Rightarrow h'(y) = 0 \Rightarrow h(y) = C$  
پاسخ: $x^2 y + x y^2 = C$

 

## 5. معادلات با عامل انتگرال‌گیر (Integrating Factor)

### 🔍 روش تشخیص

اگر معادله دقیق نیست ولی با ضرب در تابعی مانند $\mu(x)$ یا $\mu(y)$ دقیق شود، آنگاه از عامل انتگرال‌گیر استفاده می‌کنیم.

### 🛠️ روش حل

1. تشخیص اینکه $\frac{\partial N}{\partial x} - \frac{\partial M}{\partial y}$ فقط تابعی از $x$ یا $y$ است.
    
2. یافتن $\mu(x)$ یا $\mu(y)$ با استفاده از روابط مشتقی.
    
3. ضرب در عامل و سپس استفاده از روش معادله دقیق.
    

### 💡 نکات

- همیشه ابتدا بررسی کنید دقیق است یا نه.
    
- گاهی چندین انتخاب برای $\mu$ وجود دارد.
    

 

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار):

حل کنید:

$$(y+2x)dx+(x+x2)dy=0(y + 2x)dx + (x + x^2)dy = 0$$

**حل:**  
$M = y + 2x,\ N = x + x^2$  
$\frac{\partial M}{\partial y} = 1,\ \frac{\partial N}{\partial x} = 1 + 2x$  
اختلاف تابعی از $x$ است، پس عامل انتگرال‌گیر $\mu(x)$

$\mu(x) = e^{\int 2x dx} = e^{x^2}$  
ضرب و سپس استفاده از روش معادله دقیق.
***

## 6. معادلات برنولی (Bernoulli Equations)

### 🔍 روش تشخیص:
معادلات برنولی به فرم $ \frac{dy}{dx} + P(x)y = Q(x)y^n $ هستند، که در آن $n$ یک ثابت عددی است و برای $n \neq 1$ و $n \neq 0$ معادله به شکلی غیرخطی است.

### 🛠️ روش حل:
با استفاده از تغییر متغیر $v = y^{1-n}$، معادله را به یک معادله خطی تبدیل می‌کنیم و سپس آن را حل می‌کنیم.

### 💡 نکات:
- معادلات برنولی در مواردی کاربرد دارند که مدل‌سازی‌های فیزیکی شامل قدرت یا درجه خاصی از متغیرها هستند.
- برای هر مقدار از $n$ باید تبدیل متفاوتی اعمال شود.

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - مدل جمعیت):
حل کنید:  
$$
\frac{dy}{dx} + y = y^2
$$

**حل:**  
در این معادله، $n = 2$ است، بنابراین تغییر متغیر $v = y^{-1}$ را اعمال می‌کنیم.  
$ \frac{dy}{dx} = -y^{-2} \frac{dv}{dx} $  
و سپس معادله به فرم خطی تبدیل می‌شود که قابل حل است.

#### تمرین 2 (دشوار - کاربرد فیزیکی):
حل کنید:  
$$
\frac{dy}{dx} + 3y = 2y^3
$$

**حل:**  
در این معادله، $n = 3$ است و با تغییر متغیر مشابه $v = y^{-2}$، معادله را خطی کرده و سپس آن را حل می‌کنیم.

 

## 7. معادلات ریکاتی (Ricatti Equations)

### 🔍 روش تشخیص:
معادلات ریکاتی به فرم $ \frac{dy}{dx} = A(x) + B(x)y + C(x)y^2 $ هستند. این نوع معادلات معمولاً در مسائل فیزیکی پیچیده و غیرخطی مانند تحلیل‌های دینامیک و کنترل استفاده می‌شوند.

### 🛠️ روش حل:
برای حل این معادلات، ابتدا تلاش می‌کنیم تا با یک تغییر متغیر، معادله را به یک معادله درجه اول خطی تبدیل کنیم. تغییر متغیر معمولاً به شکل $ y = \frac{1}{v} $ است.

### 💡 نکات:
- معادلات ریکاتی معمولاً به‌صورت ساده قابل حل نیستند و نیاز به متدهای خاص برای حل دارند.
- به‌طور معمول، حل معادلات ریکاتی نیاز به بهره‌گیری از راه‌حل‌های خاص یا استفاده از روش‌های عددی دارد.

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - مدل کنترل):
حل کنید:  
$$
\frac{dy}{dx} = 1 + y + y^2
$$

**حل:**  
با تغییر متغیر $y = \frac{1}{v}$ معادله را خطی کرده و سپس حل می‌کنیم. تبدیل این معادله به معادله خطی معمولاً از طریق جایگذاری و فاکتورهای انتگرالی انجام می‌شود.

#### تمرین 2 (دشوار - مدل شیمیایی):
حل کنید:  
$$
\frac{dy}{dx} = 3 + 2y + 4y^2
$$

**حل:**  
با استفاده از تغییر متغیر مشابه، معادله را به فرم خطی تبدیل کرده و سپس حل می‌کنیم.

 

## 8. معادلات ولتر (Volterra Equations)

### 🔍 روش تشخیص:
این معادلات به‌ویژه در مسائل دینامیک سیستم‌ها و تحلیل‌های سیالات و گرما به کار می‌روند و به فرم $\frac{dy}{dx} = f(x) + \int_{a}^{x} g(x,t)y(t)dt$ هستند.

### 🛠️ روش حل:
برای حل این معادلات، ابتدا باید یک حل تقریبی از معادله انتگرالی پیدا کنیم. این ممکن است شامل استفاده از سری‌های تیلور یا تکنیک‌های عددی برای حل انتگرال باشد.

### 💡 نکات:
- این معادلات بیشتر در تحلیل سیستم‌های پیچیده با وابستگی زمانی و فیزیکی کاربرد دارند.
- حل این معادلات به‌طور دقیق معمولاً دشوار است و نیاز به روش‌های عددی دارند.

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - مدل پیش‌بینی سیستم):
حل کنید:  
$$
\frac{dy}{dx} = x + \int_0^x y(t)dt
$$

**حل:**  
حل این معادله معمولاً از طریق تبدیل به معادله‌ای خطی انجام می‌شود و از روش‌های سری برای حل آن استفاده می‌شود.

#### تمرین 2 (دشوار - مدل شبیه‌سازی):
حل کنید:  
$$
\frac{dy}{dx} = 2 + \int_0^x y(t)^2 dt
$$

**حل:**  
از روش‌های عددی و به‌ویژه روش‌های انتگرال‌گیری عددی استفاده می‌شود تا حل تخمینی به دست آید.

 

## 9. معادلات خود مشابه (Self-similar Equations)

### 🔍 روش تشخیص:
این معادلات معمولاً در تحلیل‌های فیزیکی و مهندسی به کار می‌روند، به‌ویژه در مسائل جریان سیالات و تحلیل‌های گرمایی. فرم آن‌ها به شکل $\frac{dy}{dx} = f(y/x)$ است.

### 🛠️ روش حل:
با استفاده از تغییر متغیرهای خود مشابه مانند $ v = y/x $، معادله به یک معادله ساده‌تر تبدیل می‌شود.

### 💡 نکات:
- معادلات خود مشابه اغلب در مسائل مربوط به رفتار مشابه در مقیاس‌های مختلف کاربرد دارند.
- این معادلات معمولاً در فیزیک و مهندسی کاربرد زیادی دارند.

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - جریان سیال):
حل کنید:  
$$
\frac{dy}{dx} = \frac{y}{x}
$$

**حل:**  
با تغییر متغیر $v = y/x$ معادله را به یک معادله ساده‌تر تبدیل کرده و سپس حل می‌کنیم.

#### تمرین 2 (دشوار - تحلیل حرارتی):
حل کنید:  
$$
\frac{dy}{dx} = \frac{y}{x^2}
$$

**حل:**  
با استفاده از تغییر متغیر مشابه، معادله را به شکلی ساده‌تر تبدیل کرده و حل می‌کنیم.

 

## 10. معادلات به‌طور خاص با متغیرهای جداسازی نشده (Non-separable equations)

### 🔍 روش تشخیص:
این معادلات معادلاتی هستند که به طور مستقیم قابل تفکیک به دو طرف $x$ و $y$ نیستند، ولی ممکن است با استفاده از تغییر متغیر یا روش‌های خاص به فرم قابل حل درآیند.

### 🛠️ روش حل:
معمولاً از روش‌هایی همچون استفاده از تبدیل متغیرهای خاص یا عوامل انتگرال‌گیر برای حل این معادلات استفاده می‌شود.

### 💡 نکات:
- این معادلات به‌طور خاص به فرم‌های پیچیده می‌آیند و نیاز به تحلیل‌های خاص دارند.
- روش‌های عددی به‌ویژه برای حل آن‌ها کاربرد دارند.

### 🧪 تمرین‌ها و حل آن‌ها

#### تمرین 1 (دشوار - تحلیل سیستم):
حل کنید:  
$$
\frac{dy}{dx} = y^2 + x
$$

**حل:**  
این معادله به‌طور ساده قابل تفکیک نیست، ولی می‌توان از روش‌های عددی برای حل استفاده کرد.

#### تمرین 2 (دشوار - مدل شبیه‌سازی):
حل کنید:  
$$
\frac{dy}{dx} = \frac{y}{x^2} + 1
$$

**حل:**  
با استفاده از تغییرات مناسب در متغیر، معادله را به یک معادله قابل حل تبدیل می‌کنیم.

 
# 🎯 دسته‌بندی جامع معادلات دیفرانسیل مرتبه دوم

## 1. بر اساس خطی بودن معادله

### 1.1. معادلات خطی (Linear Second-Order ODEs)
معادلاتی که در آن‌ها $y''$, $y'$ و $y$ به صورت خطی ظاهر می‌شوند.

- **فرم کلی:**
  $$
  a(x)y'' + b(x)y' + c(x)y = f(x)
  $$

- **زیرشاخه‌ها:**
  - با ضرایب ثابت (Constant Coefficients)
  - با ضرایب متغیر (Variable Coefficients)
  - همگن یا ناهمگن (Homogeneous / Nonhomogeneous)

 

### 1.2. معادلات غیرخطی (Nonlinear ODEs)
در این‌ها $y$, $y'$ یا $y''$ به صورت غیرخطی ظاهر می‌شوند.

- **مثال‌ها:**
  $$
  y'' + (y')^2 = 0, \quad y'' = y y'
  $$

 

## 2. بر اساس ساختار ضرایب

### 2.1. معادلات با ضرایب ثابت
$$
ay'' + by' + cy = f(x)
$$

 

### 2.2. معادلات ائولر-کوشی (Cauchy-Euler)
$$
x^2y'' + axy' + by = f(x)
$$

- قابل تبدیل به معادله با ضرایب ثابت با تغییر متغیر $x = e^t$.

 

### 2.3. معادلات با ضرایب متغیر کلی (Arbitrary Variable Coefficients)

 

## 3. بر اساس روش حل یا ساختار خاص

### 3.1. قابل کاهش به مرتبه اول
اگر $y''$ فقط به $x$ و $y'$ یا فقط به $x$ و $y$ وابسته باشد.

 

### 3.2. معادلات به فرم پارامتری
$x = x(t)$ و $y = y(t)$  
- پرکاربرد در فیزیک، دینامیک، مکانیک.

 

## 4. بر اساس نوع شرایط داده‌شده

### 4.1. مسائل مقدار اولیه (IVP)
شرایط اولیه مانند $y(x_0)$ و $y'(x_0)$ داده شده باشد.

 

### 4.2. مسائل شرایط مرزی (BVP)
شرایط در دو نقطه مانند $y(a)$ و $y(b)$ مشخص‌اند.

 

## 5. بر اساس معادلات خاص و توابع ویژه

### 5.1. معادله بسل (Bessel Equation)
$$
x^2y'' + xy' + (x^2 - n^2)y = 0
$$

 

### 5.2. معادله لاگندر (Legendre Equation)
$$
(1 - x^2)y'' - 2xy' + n(n+1)y = 0
$$

 

### 5.3. سایر معادلات خاص
- معادله ایر (Airy)
- معادله هرمیت (Hermite)
- معادله لاپلاس (Laplace)
- معادله لاگرانژ
- معادله هلمهولتز و ...

 

## 6. معادلات با تقارن‌های خاص
- معادلات کروی
- معادلات استوانه‌ای
- معادلات با ساختار متقارن حاصل از فیزیک

 

## 7. معادلات دیفرانسیل در فیزیک کوانتومی
- مانند معادله شرودینگر با پتانسیل‌های خاص
- معمولاً منجر به معادلات ویژه و توابع خاص می‌شوند

 
# چشم انداز
هر دسته از معادلات دیفرانسیل مرتبه دوم را به‌صورت کامل با ساختار زیر ادامه می دهیم:
- ✅ عنوان دسته
- 🔎 روش‌های تشخیص
- 🛠️ روش‌های حل گام‌به‌گام
- 💡 نکات کلیدی و دام‌های رایج
- 🧠 تمرینات دشوار و متنوع همراه با **حل کاملاً تشریحی**
 
# 📚 دسته‌بندی معادلات دیفرانسیل مرتبه دوم بر اساس روش حل

 

## 1. معادلات همگن با ضرایب ثابت (Homogeneous with Constant Coefficients)

- **فرم کلی:**
  $$
  ay'' + by' + cy = 0
  $$
- **مثال:**
  $$
  y'' - 4y' + 4y = 0
  $$

 

## 2. معادلات غیرهمگن با ضرایب ثابت (Nonhomogeneous with Constant Coefficients)

- **فرم کلی:**
  $$
  ay'' + by' + cy = f(x)
  $$
- **مثال:**
  $$
  y'' + y = \cos x
  $$

 

## 3. معادلات کوشی-اویلار (Cauchy-Euler)

- **فرم کلی:**
  $$
  x^2 y'' + a x y' + b y = f(x)
  $$
- **مثال:**
  $$
  x^2 y'' - 3x y' + 4y = 0
  $$

 

## 4. معادلات قابل کاهش مرتبه (Reducible Order)

- **مثال:**
  $$
  y'' = f(y')
  $$

 

## 5. معادلات با متغیر وابسته حذف‌شده (No $y$ Present)

- **مثال:**
  $$
  y'' = f(x, y')
  $$

 

## 6. معادلات با متغیر مستقل حذف‌شده (No $x$ Present)

- **مثال:**
  $$
  y'' = f(y, y')
  $$

 

## 7. معادلات قابل تجزیه به دو معادله مرتبه اول

- **مثال:**
  $$
  y'' = f(x, y, y') \quad \Rightarrow \quad 
  \begin{cases}
  y' = u \\
  u' = f(x, y, u)
  \end{cases}
  $$

 

## 8. معادلات خطی با ضرایب متغیر (Linear with Variable Coefficients)

- **مثال:**
  $$
  (1 + x^2)y'' + x y' + y = 0
  $$

 

## 9. معادلات قابل حل با سری توانی (Power Series Method)

- **مثال:**
  $$
  y'' + x y' + y = 0
  $$

 

## 10. معادلات قابل حل با تغییر متغیر خاص یا تابع ویژه

- **مثال:**
  $$
  x^2y'' + xy' + (x^2 - \nu^2)y = 0 \quad \text{(معادله بسل)}
  $$

 

## 11. معادلات قابل حل عددی (Numerical Methods)

- **مثال:**
  $$
  y'' + \ln(x)y = e^x
  $$

 
# 📘 معادلات دیفرانسیل مرتبه دوم همگن با ضرایب ثابت

 

## 🔎 روش تشخیص

معادله‌ای از فرم کلی زیر:

$$
a y'' + b y' + c y = 0 \quad \text{(که در آن } a, b, c \in \mathbb{R} \text{ ثابت هستند)}
$$

این معادله **همگن** است چون سمت راست برابر صفر است و **ضرایب ثابت** دارد چون $a$، $b$ و $c$ اعداد حقیقی ثابتی هستند.

 

## 🛠️ روش حل

1. نوشتن **معادله مشخصه (Characteristic Equation)**:

   معادله زیر را تشکیل می‌دهیم:

   $$
   a r^2 + b r + c = 0
   $$

2. حل معادله درجه دوم بالا. بسته به دلتا ($\Delta = b^2 - 4ac$)، سه حالت پیش می‌آید:

 

### ✅ حالت ۱: ریشه‌های حقیقی و متمایز ($\Delta > 0$)

اگر $r_1$ و $r_2$ ریشه‌های متمایز باشند:

$$
y(x) = C_1 e^{r_1 x} + C_2 e^{r_2 x}
$$

 

### ✅ حالت ۲: ریشه‌های حقیقی و مساوی ($\Delta = 0$)

اگر $r$ ریشه دوگانه باشد:

$$
y(x) = (C_1 + C_2 x) e^{r x}
$$

 

### ✅ حالت ۳: ریشه‌های مختلط ($\Delta < 0$)

اگر ریشه‌ها به صورت $r = \alpha \pm i\beta$ باشند:

$$
y(x) = e^{\alpha x}(C_1 \cos \beta x + C_2 \sin \beta x)
$$

 

## 💡 نکات کلیدی و دام‌های رایج

- هنگام تشکیل معادله مشخصه، فراموش نکنید ضریب $a$ را صفر فرض نکنید. اگر $a=0$، دیگر معادله از درجه دوم نیست.
- اگر ریشه‌ها مختلط هستند، پاسخ باید **حقیقی** نوشته شود؛ به همین دلیل از $\cos$ و $\sin$ استفاده می‌کنیم.
- اشتباه رایج: فراموش‌کردن فاکتور $e^{\alpha x}$ در حالت مختلط.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: حالت ریشه‌های مختلط

حل کنید:

$$
y'' - 4y' + 13y = 0
$$

**حل:**

- معادله مشخصه:

  $$
  r^2 - 4r + 13 = 0
  $$

  $$
  \Delta = (-4)^2 - 4(1)(13) = 16 - 52 = -36
  $$

  $$
  r = \frac{4 \pm \sqrt{-36}}{2} = 2 \pm 3i
  $$

- جواب عمومی:

  $$
  y(x) = e^{2x}(C_1 \cos 3x + C_2 \sin 3x)
  $$

 

### 🧪 تمرین ۲: ریشه‌های تکراری

حل کنید:

$$
y'' - 6y' + 9y = 0
$$

**حل:**

- معادله مشخصه:

  $$
  r^2 - 6r + 9 = 0 \quad \Rightarrow \quad (r - 3)^2 = 0
  $$

- تنها ریشه: $r = 3$

- جواب عمومی:

  $$
  y(x) = (C_1 + C_2 x) e^{3x}
  $$

 

### 🧪 تمرین ۳: ریشه‌های حقیقی و متمایز

حل کنید:

$$
y'' - y' - 6y = 0
$$

**حل:**

- معادله مشخصه:

  $$
  r^2 - r - 6 = 0 \quad \Rightarrow \quad (r - 3)(r + 2) = 0
  $$

  $$
  r_1 = 3, \quad r_2 = -2
  $$

- جواب عمومی:

  $$
  y(x) = C_1 e^{3x} + C_2 e^{-2x}
  $$

 

### 🧪 تمرین ۴: سؤال مفهومی – تأثیر شرط اولیه

با توجه به معادله زیر:

$$
y'' + 4y = 0, \quad y(0) = 2, \quad y'(0) = 0
$$

**حل:**

- مشخصه: $r^2 + 4 = 0 \Rightarrow r = \pm 2i$
- پاسخ عمومی:

  $$
  y(x) = C_1 \cos 2x + C_2 \sin 2x
  $$

- اعمال شرایط اولیه:

  - $y(0) = 2 \Rightarrow C_1 = 2$
  - $y'(x) = -2C_1 \sin 2x + 2C_2 \cos 2x \Rightarrow y'(0) = 2C_2 = 0 \Rightarrow C_2 = 0$

- پاسخ نهایی:

  $$
  y(x) = 2 \cos 2x
  $$

 

### 🧪 تمرین ۵: تحلیل ریشه‌های مختلط نزدیک به صفر

حل کنید:

$$
y'' + 0.0001y' + 25y = 0
$$

**حل:**

- مشخصه: $r^2 + 0.0001r + 25 = 0$
- $\Delta = (0.0001)^2 - 4(1)(25) \approx -100 \Rightarrow$ ریشه‌ها مختلط‌اند.
- $r = \frac{-0.0001}{2} \pm i \sqrt{25} = -0.00005 \pm 5i$

- پاسخ:

  $$
  y(x) = e^{-0.00005x}(C_1 \cos 5x + C_2 \sin 5x)
  $$

> 🔍 **نکته:** ضریب نمایی بسیار کوچک باعث می‌شود نوسان‌ها تقریباً بدون میرایی باشند؛ این رفتار برای تحلیل سیستم‌های تقریباً پایدار مهم است.

 
# 📘 معادلات دیفرانسیل مرتبه دوم غیرهمگن با ضرایب ثابت

 

## 🔎 روش تشخیص

معادله‌ای از فرم کلی زیر:

$$
a y'' + b y' + c y = f(x)
$$

که در آن $a$، $b$ و $c$ ثابت هستند و $f(x)$ یک تابع غیرصفر است، یک معادله **غیرهمگن** با ضرایب ثابت است.

 

## 🛠️ روش حل

برای حل این نوع معادلات ابتدا باید **حل عمومی** و سپس **حل خاص** را پیدا کنیم.

### 1. **حل همگن معادله (Homogeneous Solution)**

با همان روشی که برای معادله همگن ذکر شد (در دسته قبلی)، معادله مشخصه را می‌سازیم:

$$
a r^2 + b r + c = 0
$$

حل معادله مشخصه به ما **حل همگن** معادله را می‌دهد.

### 2. **حل خاص معادله (Particular Solution)**

برای پیدا کردن **حل خاص**، روش‌هایی مانند **روش ضرایب نامعین**، **روش تغییر تابع مجهول** یا **روش انتگرال‌گیری** به کار می‌روند. انتخاب روش بستگی به فرم تابع غیرهمگن $f(x)$ دارد.

#### روش ضرایب نامعین (Method of Undetermined Coefficients)

1. فرض می‌کنیم که فرم حل خاص مشابه فرم تابع $f(x)$ است.
2. ضرایب نامعین را تعیین کرده و معادله را حل می‌کنیم.

#### روش تغییر تابع مجهول (Method of Variation of Parameters)

1. فرض می‌کنیم که حل خاص به فرم $y_p = u_1(x) y_1(x) + u_2(x) y_2(x)$ است.
2. معادلات جدید برای $u_1$ و $u_2$ حل می‌شوند.

 

## 💡 نکات کلیدی و دام‌های رایج

- **حل همگن و خاص باید ترکیب شوند.** همیشه بعد از پیدا کردن حل همگن، حل خاص را به آن اضافه کنید.
- در روش **ضرایب نامعین**، اگر تابع غیرهمگن مشابه یکی از اجزای حل همگن باشد، باید ضریب‌های نامعین را در فرم جدیدی جستجو کرد (مثلاً به‌وسیله $x$ ضرب کردن).
- **در روش تغییر تابع مجهول**، در صورتی که معادله همگن حل ساده‌ای نداشته باشد، ممکن است نیاز به استفاده از روش‌های پیچیده‌تر برای تعیین $u_1(x)$ و $u_2(x)$ باشد.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: معادله غیرهمگن با ضرایب ثابت

حل کنید:

$$
y'' - 3y' + 2y = e^{2x}
$$

**حل:**

- معادله مشخصه: $r^2 - 3r + 2 = 0$
  
  ریشه‌ها: $r_1 = 1$, $r_2 = 2$
  
  بنابراین، حل همگن معادله: 

  $$
  y_h(x) = C_1 e^x + C_2 e^{2x}
  $$

- برای حل خاص، چون تابع غیرهمگن $e^{2x}$ مشابه یکی از اجزای حل همگن است، فرض می‌کنیم:

  $$
  y_p(x) = A x e^{2x}
  $$

- مشتقات:
  $$
  y_p' = A e^{2x} + 2Ax e^{2x}
  $$
  $$
  y_p'' = 4Ax e^{2x} + 4A e^{2x}
  $$

- جایگذاری در معادله اصلی:
  $$
  4Ax e^{2x} + 4A e^{2x} - 3(A e^{2x} + 2Ax e^{2x}) + 2A x e^{2x} = e^{2x}
  $$

  حل برای $A$: $A = \frac{1}{2}$

- جواب خاص:

  $$
  y_p(x) = \frac{1}{2} x e^{2x}
  $$

- جواب کلی:

  $$
  y(x) = C_1 e^x + C_2 e^{2x} + \frac{1}{2} x e^{2x}
  $$

 

### 🧪 تمرین ۲: معادله غیرهمگن با تغییرات پیچیده‌تر

حل کنید:

$$
y'' + 4y = \sin(2x)
$$

**حل:**

- معادله مشخصه: $r^2 + 4 = 0$
  
  ریشه‌ها: $r = \pm 2i$
  
  بنابراین، حل همگن معادله:

  $$
  y_h(x) = C_1 \cos(2x) + C_2 \sin(2x)
  $$

- برای حل خاص، چون تابع غیرهمگن $\sin(2x)$ است، فرض می‌کنیم:

  $$
  y_p(x) = A x \cos(2x) + B x \sin(2x)
  $$

- مشتقات:
  $$
  y_p' = A \cos(2x) - 2A x \sin(2x) + B \sin(2x) + 2B x \cos(2x)
  $$
  $$
  y_p'' = -4A x \cos(2x) - 4A \sin(2x) - 4B x \sin(2x) + 4B \cos(2x)
  $$

- جایگذاری در معادله اصلی:

  $$
  (-4A x \cos(2x) - 4A \sin(2x) - 4B x \sin(2x) + 4B \cos(2x)) + 4(A x \cos(2x) + B x \sin(2x)) = \sin(2x)
  $$

  حل برای $A$ و $B$: $A = 0$, $B = -\frac{1}{8}$

- جواب خاص:

  $$
  y_p(x) = -\frac{1}{8} x \sin(2x)
  $$

- جواب کلی:

  $$
  y(x) = C_1 \cos(2x) + C_2 \sin(2x) - \frac{1}{8} x \sin(2x)
  $$

 

### 🧪 تمرین ۳: معادله با تابع نمایی

حل کنید:

$$
y'' + 2y' + 5y = e^{-x} \cos(2x)
$$

**حل:**

- معادله مشخصه: $r^2 + 2r + 5 = 0$
  
  ریشه‌ها: $r = -1 \pm 2i$
  
  بنابراین، حل همگن معادله:

  $$
  y_h(x) = e^{-x}(C_1 \cos(2x) + C_2 \sin(2x))
  $$

- برای حل خاص، چون تابع غیرهمگن به صورت $e^{-x} \cos(2x)$ است، فرض می‌کنیم:

  $$
  y_p(x) = e^{-x}(A \cos(2x) + B \sin(2x))
  $$

- مشتقات:
  $$
  y_p' = e^{-x}(-A \cos(2x) - 2A \sin(2x) - B \sin(2x) + 2B \cos(2x))
  $$
  $$
  y_p'' = e^{-x}(2A \cos(2x) - 4A \sin(2x) - 2B \sin(2x) - 4B \cos(2x))
  $$

- جایگذاری در معادله اصلی و حل برای $A$ و $B$: $A = \frac{3}{13}$, $B = \frac{2}{13}$

- جواب خاص:

  $$
  y_p(x) = e^{-x} \left( \frac{3}{13} \cos(2x) + \frac{2}{13} \sin(2x) \right)
  $$

- جواب کلی:

  $$
  y(x) = e^{-x} \left( C_1 \cos(2x) + C_2 \sin(2x) + \frac{3}{13} \cos(2x) + \frac{2}{13} \sin(2x) \right)
  $$

 


# 📘 معادلات دیفرانسیل مرتبه دوم کوشی-اویلار (Cauchy-Euler)

 

## 🔎 روش تشخیص

معادله‌ای از فرم کلی زیر:

$$
a x^2 y'' + b x y' + c y = f(x)
$$

که در آن $a$، $b$ و $c$ ثابت هستند و $f(x)$ تابع غیرصفر است، یک معادله **Cauchy-Euler** است. این نوع معادلات به‌طور خاص به دلیل حضور $x^2$ و $x$ در مشتقات دوم و اول شناخته می‌شوند.

 

## 🛠️ روش حل

برای حل این نوع معادلات، ابتدا باید معادله همگن را حل کنیم و سپس به دنبال حل خاص برای بخش غیرهمگن باشیم.

### 1. **حل همگن معادله (Homogeneous Solution)**

برای حل همگن، ابتدا معادله مشخصه را می‌سازیم. معادله مشخصه به‌صورت زیر است:

$$
a r^2 + b r + c = 0
$$

این معادله را حل کرده و ریشه‌های آن را پیدا می‌کنیم. بسته به نوع ریشه‌ها، روش حل همگن تغییر می‌کند:

- **ریشه‌های حقیقی مختلف**: فرم حل همگن به صورت $y_h = C_1 x^{r_1} + C_2 x^{r_2}$ خواهد بود.
- **ریشه‌های تکراری**: فرم حل همگن به صورت $y_h = C_1 x^r + C_2 x^r \ln(x)$ خواهد بود.
- **ریشه‌های مختلط**: فرم حل همگن به صورت $y_h = x^{\alpha} (C_1 \cos(\beta \ln(x)) + C_2 \sin(\beta \ln(x)))$ خواهد بود، که $\alpha$ و $\beta$ از ریشه‌های معادله مشخصه استخراج می‌شوند.

### 2. **حل خاص معادله (Particular Solution)**

برای پیدا کردن **حل خاص** معادله، بستگی به نوع تابع غیرهمگن $f(x)$ دارد. روش‌های مختلفی برای یافتن حل خاص وجود دارند:

- **روش ضرایب نامعین (Method of Undetermined Coefficients)**: این روش بیشتر زمانی استفاده می‌شود که تابع غیرهمگن از نوع تابع نمایی، مثلثاتی، یا چندجمله‌ای باشد.
- **روش تغییر تابع مجهول (Method of Variation of Parameters)**: برای توابع غیرهمگن پیچیده‌تر یا زمانی که روش ضرایب نامعین قابل استفاده نیست.

 

## 💡 نکات کلیدی و دام‌های رایج

- **توجه به تغییرات در ریشه‌های معادله مشخصه**: اگر ریشه‌ها تکراری یا مختلط باشند، فرم حل همگن تغییر خواهد کرد.
- **در روش ضرایب نامعین**، اگر فرم تابع غیرهمگن مشابه با یکی از اجزای حل همگن باشد، باید به‌وسیله $x$ ضرب کردن آن، ضریب‌های نامعین را بیابید.
- **در روش تغییر تابع مجهول**، معادله‌هایی که ضرایب آن‌ها تغییر می‌کنند (مثل معادلات غیرهمگن) ممکن است نیاز به روش‌های پیچیده‌تر برای تعیین $u_1(x)$ و $u_2(x)$ داشته باشند.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: معادله کوشی-اویلار

حل کنید:

$$
x^2 y'' - 4x y' + 6y = 0
$$

**حل:**

- معادله مشخصه: $r^2 - 5r + 6 = 0$
  
  ریشه‌ها: $r_1 = 2$, $r_2 = 3$
  
  بنابراین، حل همگن معادله:

  $$
  y_h(x) = C_1 x^2 + C_2 x^3
  $$

- معادله همگن به پایان رسید زیرا تابع غیرهمگنی وجود ندارد.

- جواب کلی:

  $$
  y(x) = C_1 x^2 + C_2 x^3
  $$

 

### 🧪 تمرین ۲: معادله کوشی-اویلار غیرهمگن

حل کنید:

$$
x^2 y'' + 3x y' + 2y = x^2 \ln(x)
$$

**حل:**

- معادله مشخصه: $r^2 + 3r + 2 = 0$
  
  ریشه‌ها: $r_1 = -1$, $r_2 = -2$
  
  بنابراین، حل همگن معادله:

  $$
  y_h(x) = C_1 x^{-1} + C_2 x^{-2}
  $$

- برای پیدا کردن حل خاص، چون تابع غیرهمگن $x^2 \ln(x)$ است، فرض می‌کنیم که:

  $$
  y_p(x) = A x^2 \ln(x) + B x^2
  $$

- مشتقات:
  $$
  y_p' = 2Ax \ln(x) + Ax + 2Bx
  $$
  $$
  y_p'' = 2A \ln(x) + 4Ax + 2B
  $$

- جایگذاری در معادله اصلی:

  $$
  x^2 (2A \ln(x) + 4Ax + 2B) + 3x (2Ax \ln(x) + Ax + 2Bx) + 2 (Ax^2 \ln(x) + Bx^2) = x^2 \ln(x)
  $$

  حل برای $A$ و $B$: $A = \frac{1}{2}$، $B = -\frac{1}{2}$

- جواب خاص:

  $$
  y_p(x) = \frac{1}{2} x^2 \ln(x) - \frac{1}{2} x^2
  $$

- جواب کلی:

  $$
  y(x) = C_1 x^{-1} + C_2 x^{-2} + \frac{1}{2} x^2 \ln(x) - \frac{1}{2} x^2
  $$

 

### 🧪 تمرین ۳: معادله کوشی-اویلار با تابع نمایی

حل کنید:

$$
x^2 y'' - 5x y' + 6y = e^{x}
$$

**حل:**

- معادله مشخصه: $r^2 - 5r + 6 = 0$
  
  ریشه‌ها: $r_1 = 2$, $r_2 = 3$
  
  بنابراین، حل همگن معادله:

  $$
  y_h(x) = C_1 x^2 + C_2 x^3
  $$

- برای حل خاص، چون تابع غیرهمگن $e^x$ است، فرض می‌کنیم که:

  $$
  y_p(x) = A e^x
  $$

- مشتقات:
  $$
  y_p' = A e^x
  $$
  $$
  y_p'' = A e^x
  $$

- جایگذاری در معادله اصلی:

  $$
  x^2 A e^x - 5x A e^x + 6A e^x = e^x
  $$

  حل برای $A$: $A = \frac{1}{6}$

- جواب خاص:

  $$
  y_p(x) = \frac{1}{6} e^x
  $$

- جواب کلی:

  $$
  y(x) = C_1 x^2 + C_2 x^3 + \frac{1}{6} e^x
  $$

 

# 📘 معادلات دیفرانسیل مرتبه دوم قابل کاهش مرتبه (Reducible Order)

 

## 🔎 روش تشخیص

معادله‌ای از فرم کلی زیر:

$$
y'' + p(x) y' + q(x) y = 0
$$

که در آن $p(x)$ و $q(x)$ توابع معلوم هستند، زمانی قابل کاهش مرتبه است که بتوان آن را به یک معادله مرتبه اول تبدیل کرد. برای این‌که یک معادله مرتبه دوم قابل کاهش مرتبه باشد، باید حداقل یکی از شرایط زیر برقرار باشد:

1. معادله باید دارای یک راه‌حل خاص باشد که به ما کمک کند تا مرتبه معادله را کاهش دهیم.
2. این معادلات معمولاً در زمانی قابل استفاده هستند که راه‌حل عمومی معادله دوم از یک راه‌حل خاص حاصل شود.

 

## 🛠️ روش حل

برای حل معادلات قابل کاهش مرتبه، ابتدا به دنبال پیدا کردن یک راه‌حل خاص $y_1(x)$ می‌گردیم. پس از پیدا کردن این راه‌حل، می‌توانیم معادله را به یک معادله مرتبه اول کاهش دهیم.

### 1. **پیدا کردن راه‌حل خاص $y_1(x)$**

ابتدا راه‌حل خاص $y_1(x)$ را برای معادله مرتبه دوم پیدا می‌کنیم. این کار معمولاً با استفاده از روش‌های معمول حل معادلات دیفرانسیل مرتبه دوم (مثل حل همگن) انجام می‌شود.

### 2. **کاهش مرتبه معادله**

پس از یافتن یک راه‌حل خاص $y_1(x)$، معادله را به یک معادله مرتبه اول تبدیل می‌کنیم. برای این کار می‌توانیم فرض کنیم که حل عمومی معادله به صورت $y(x) = y_1(x) v(x)$ باشد، که در آن $v(x)$ تابعی است که باید پیدا شود.

### 3. **حل معادله مرتبه اول**

سپس معادله مرتبه اول برای $v(x)$ را حل می‌کنیم. این معادله معمولاً یک معادله تفکیک‌پذیر خواهد بود که می‌توان آن را حل کرد.

 

## 💡 نکات کلیدی و دام‌های رایج

- **یافتن راه‌حل خاص**: در برخی از معادلات، یافتن یک راه‌حل خاص ممکن است پیچیده باشد. اگر راه‌حل خاص به‌درستی پیدا نشود، کاهش مرتبه به درستی انجام نمی‌شود.
- **روش $y(x) = y_1(x) v(x)$**: توجه به این نکته ضروری است که $y_1(x)$ باید یک راه‌حل خاص واقعی باشد تا بتوانیم معادله را کاهش مرتبه دهیم.
- **عدم تشخیص راحت**: برخی از معادلات که به‌نظر می‌رسد قابل کاهش مرتبه هستند، ممکن است در حقیقت نیاز به روش‌های پیچیده‌تر داشته باشند که شبیه به معادلات غیرخطی باشند.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: معادله قابل کاهش مرتبه

حل کنید:

$$
y'' + \frac{2}{x} y' + \frac{1}{x^2} y = 0
$$

**حل:**

1. ابتدا فرض می‌کنیم که معادله دارای یک راه‌حل خاص از نوع $y_1(x) = \frac{1}{x}$ است.
2. سپس معادله را به صورت $y(x) = \frac{1}{x} v(x)$ می‌نویسیم.
3. مشتقات را محاسبه می‌کنیم:
   $$
   y' = \frac{v'}{x} - \frac{v}{x^2}, \quad y'' = \frac{v''}{x} - \frac{2v'}{x^2} + \frac{2v}{x^3}
   $$
4. معادله اصلی را جایگذاری می‌کنیم و پس از ساده‌سازی، به معادله مرتبه اول برای $v(x)$ می‌رسیم:
   $$
   v'' = 0
   $$
5. حل معادله مرتبه اول:
   $$
   v(x) = C_1 + C_2 x
   $$
6. بنابراین، جواب کلی معادله به صورت زیر خواهد بود:
   $$
   y(x) = \frac{1}{x} (C_1 + C_2 x)
   $$

 

### 🧪 تمرین ۲: معادله قابل کاهش مرتبه غیرهمگن

حل کنید:

$$
y'' + \frac{3}{x} y' + \frac{2}{x^2} y = x \ln(x)
$$

**حل:**

1. ابتدا فرض می‌کنیم که معادله همگن را حل کنیم. معادله مشخصه برای معادله همگن به شکل زیر است:
   $$
   r^2 + 2r + 2 = 0
   $$ 
   که ریشه‌ها $r = -1 \pm i$ می‌باشند. بنابراین، حل همگن معادله به‌صورت زیر خواهد بود:
   $$
   y_h(x) = C_1 x^{-1} \cos(\ln(x)) + C_2 x^{-1} \sin(\ln(x))
   $$

2. برای پیدا کردن حل خاص، فرض می‌کنیم که $y_p(x) = A x \ln(x)$.
3. مشتقات را محاسبه می‌کنیم:
   $$
   y_p' = A \ln(x) + A, \quad y_p'' = \frac{A}{x}
   $$
4. معادله اصلی را جایگذاری می‌کنیم:
   $$
   \frac{A}{x} + \frac{3}{x} (A \ln(x) + A) + \frac{2}{x^2} A x \ln(x) = x \ln(x)
   $$
   پس از ساده‌سازی و حل برای $A$:
   $$
   A = 1
   $$

5. بنابراین، جواب خاص معادله به صورت زیر خواهد بود:
   $$
   y_p(x) = x \ln(x)
   $$

6. جواب کلی معادله:

   $$
   y(x) = C_1 x^{-1} \cos(\ln(x)) + C_2 x^{-1} \sin(\ln(x)) + x \ln(x)
   $$

 

### 🧪 تمرین ۳: معادله قابل کاهش مرتبه با ضرایب متغیر

حل کنید:

$$
x^2 y'' - 4x y' + 6y = 0
$$

**حل:**

1. معادله مشخصه:
   $$
   r^2 - 5r + 6 = 0
   $$
   ریشه‌ها: $r_1 = 2$, $r_2 = 3$
2. بنابراین، حل همگن معادله:
   $$
   y_h(x) = C_1 x^2 + C_2 x^3
   $$
3. معادله به حالت همگن تبدیل شد و نیازی به کاهش مرتبه نداشت.

 

# 5. معادلات با متغیر وابسته حذف‌شده (No $y$ Present)

 

## 🔎 روش تشخیص

اگر معادله دیفرانسیل مرتبه دوم به‌صورت زیر باشد:

$$
y'' = f(x, y')
$$

و **$y$ در هیچ‌کجای معادله ظاهر نشود** (یعنی تنها $x$ و $y'$ حضور دارند)، می‌توان گفت که این معادله از نوع "با متغیر وابسته حذف‌شده" است. در این‌گونه معادلات، می‌توان با تغییر متغیر $p = y'$ معادله را به معادله مرتبه اول تبدیل کرد.

 

## 🛠️ روش حل

1. **تغییر متغیر:**
   فرض می‌کنیم:

   $$
   p = y' \quad \Rightarrow \quad y'' = p'
   $$

   اما چون $p$ تابعی از $x$ است و $y'' = \frac{dp}{dx}$، می‌توان مستقیماً نوشت:

   $$
   \frac{dp}{dx} = f(x, p)
   $$

2. **حل معادله مرتبه اول:**
   حالا معادله به یک معادله مرتبه اول برای $p(x)$ تبدیل شده است.

3. **بازگشت به $y$:**
   با داشتن $p(x) = y'(x)$، از انتگرال‌گیری استفاده می‌کنیم:

   $$
   y(x) = \int p(x) dx + C
   $$

 

## 💡 نکات کلیدی و دام‌های رایج

- این روش **فقط زمانی قابل اعمال است که $y$ در معادله ظاهر نشود**. اگر $y$ در هرجای معادله باشد، روش کار نمی‌کند.
- حل معادله $p' = f(x, p)$ ممکن است ساده یا دشوار باشد؛ بسته به نوع تابع $f$ می‌توان از تفکیک‌پذیر، خطی یا فاکتور انتگرال‌گیر استفاده کرد.
- پس از حل $p(x)$، مرحله‌ی دوم یعنی یافتن $y(x)$ بسیار ساده است، ولی در برخی موارد ممکن است انتگرال نهایی نیازمند روش‌های خاص باشد.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: معادله غیرخطی تفکیک‌پذیر

حل کنید:

$$
y'' = (y')^2 + x
$$

**حل:**

1. تغییر متغیر:

   $$
   p = y' \quad \Rightarrow \quad p' = (p)^2 + x
   $$

2. حالا یک معادله دیفرانسیل مرتبه اول برای $p(x)$ داریم:

   $$
   \frac{dp}{dx} = p^2 + x
   $$

   این معادله غیرخطی و غیرتفکیک‌پذیر است، ولی می‌توان آن را با روش عددی یا سری تیلور حل کرد. در این‌جا فقط ساختار کلی راه حل را بیان می‌کنیم.

3. فرض کنیم راه حل خاصی مثل $p(x) = ax + b$ داشته باشیم. با جایگذاری و مقایسه ضرایب (روش آزمون و خطا)، اگر پیدا نشد، به حل عددی رجوع می‌کنیم.

4. پس از یافتن $p(x)$، انتگرال می‌گیریم:

   $$
   y(x) = \int p(x) dx + C
   $$

 

### 🧪 تمرین ۲: معادله خطی با ضرایب متغیر

حل کنید:

$$
y'' = \frac{2}{x} y'
$$

**حل:**

1. تغییر متغیر:

   $$
   p = y' \Rightarrow \frac{dp}{dx} = \frac{2}{x} p
   $$

   این معادله خطی است:

   $$
   \frac{dp}{dx} - \frac{2}{x} p = 0
   $$

2. حل با روش فاکتور انتگرال‌گیر:

   فاکتور: $\mu(x) = e^{\int -\frac{2}{x} dx} = x^{-2}$

   پس:

   $$
   \frac{d}{dx}(p x^{-2}) = 0 \Rightarrow p x^{-2} = C_1 \Rightarrow p = C_1 x^2
   $$

3. حالا:

   $$
   y' = C_1 x^2 \Rightarrow y = \int C_1 x^2 dx = \frac{C_1}{3} x^3 + C_2
   $$

✅ **جواب نهایی:**

$$
y(x) = \frac{C_1}{3} x^3 + C_2
$$

 

### 🧪 تمرین ۳: معادله قابل تجزیه به صورت خاص

حل کنید:

$$
y'' = \frac{(y')^2}{x}
$$

**حل:**

1. تغییر متغیر:

   $$
   p = y' \Rightarrow p' = \frac{p^2}{x}
   $$

   این معادله تفکیک‌پذیر است:

   $$
   \frac{dp}{p^2} = \frac{dx}{x}
   $$

2. انتگرال دو طرف:

   $$
   -\frac{1}{p} = \ln|x| + C_1 \Rightarrow p = -\frac{1}{\ln|x| + C_1}
   $$

3. حالا:

   $$
   y' = p = -\frac{1}{\ln|x| + C_1}
   $$

4. انتگرال نهایی:

   $$
   y(x) = \int -\frac{1}{\ln|x| + C_1} dx
   $$

   این انتگرال به صورت بسته قابل حل نیست و نیاز به روش‌های عددی یا تقریبی دارد.

 

# 6. معادلات با متغیر مستقل حذف‌شده (No $x$ Present)

 

## 🔎 روش تشخیص

اگر معادله دیفرانسیل مرتبه دوم به‌صورت زیر باشد:

$$
y'' = f(y, y')
$$

و **$x$ در معادله ظاهر نشود** (یعنی تنها $y$ و $y'$ حضور دارند)، می‌توان با تغییر متغیر مناسب، این معادله را به یک معادله مرتبه اول تبدیل کرد.

 

## 🛠️ روش حل

1. فرض می‌کنیم:

   $$
   p = y' = \frac{dy}{dx}
   $$

2. از قانون زنجیره‌ای می‌دانیم:

   $$
   y'' = \frac{dp}{dx} = \frac{dp}{dy} \cdot \frac{dy}{dx} = p \frac{dp}{dy}
   $$

3. بنابراین معادله به شکل زیر تبدیل می‌شود:

   $$
   p \frac{dp}{dy} = f(y, p)
   $$

4. حالا یک **معادله مرتبه اول برای $p(y)$** داریم. آن را حل می‌کنیم.

5. در پایان با انتگرال‌گیری از $p = \frac{dy}{dx}$، به $y(x)$ می‌رسیم.

 

## 💡 نکات کلیدی و دام‌های رایج

- این روش **فقط زمانی قابل استفاده است که $x$ در معادله وجود نداشته باشد**.
- گام مهم، استفاده از قانون زنجیره‌ای برای تبدیل $y''$ به $p \frac{dp}{dy}$ است.
- حل نهایی گاهی نیازمند دقت در انتگرال‌گیری است و ممکن است به روش‌های خاص یا عددی نیاز داشته باشد.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: معادله مربعی در $y'$

حل کنید:

$$
y'' = (y')^2 + y
$$

**حل:**

1. تغییر متغیر:

   $$
   p = y' \quad \Rightarrow \quad y'' = p \frac{dp}{dy}
   $$

   جایگذاری:

   $$
   p \frac{dp}{dy} = p^2 + y
   $$

2. معادله قابل تفکیک نیست. یک روش پیشنهادی: تقسیم دو طرف بر $p$:

   $$
   \frac{dp}{dy} = p + \frac{y}{p}
   $$

   این معادله از نوع غیرخطی و سخت است و نیازمند تکنیک‌های خاص یا حل عددی است.

3. اگر تابع خاصی حدس بزنیم (مثلاً $p = a y$)، با جایگذاری و مقایسه ضرایب می‌توان تست کرد که آیا جواب خاصی دارد یا خیر.

 

### 🧪 تمرین ۲: معادله قابل تفکیک

حل کنید:

$$
y'' = y'
$$

**حل:**

1. تغییر متغیر:

   $$
   p = y' \quad \Rightarrow \quad y'' = p \frac{dp}{dy}
   $$

   جایگذاری:

   $$
   p \frac{dp}{dy} = p \Rightarrow \frac{dp}{dy} = 1
   $$

2. انتگرال‌گیری:

   $$
   p = y + C_1
   $$

3. از $p = \frac{dy}{dx} = y + C_1$:

   $$
   \frac{dy}{dx} = y + C_1
   $$

   معادله تفکیک‌پذیر:

   $$
   \frac{dy}{y + C_1} = dx
   $$

4. انتگرال می‌گیریم:

   $$
   \ln|y + C_1| = x + C_2 \Rightarrow y + C_1 = Ce^x
   $$

✅ **جواب نهایی:**

$$
y(x) = Ce^x - C_1
$$

 

### 🧪 تمرین ۳: معادله با لگاریتم

حل کنید:

$$
y'' = \ln(y')
$$

**حل:**

1. تغییر متغیر:

   $$
   p = y' \Rightarrow y'' = p \frac{dp}{dy}
   $$

   پس:

   $$
   p \frac{dp}{dy} = \ln(p)
   $$

2. حل این معادله پیچیده است. تقسیم دو طرف بر $p$:

   $$
   \frac{dp}{dy} = \frac{\ln(p)}{p}
   $$

   این معادله از نوع برنولی نیست ولی قابل حل عددی یا با تغییر متغیر است. مثلاً:

   بگذارید $u = \ln(p) \Rightarrow p = e^u, \quad dp = e^u du$

   بنابراین:

   $$
   \frac{dp}{dy} = \frac{\ln(p)}{p} \Rightarrow e^u \frac{du}{dy} = \frac{u}{e^u} \Rightarrow \frac{du}{dy} = \frac{u}{e^{2u}}
   $$

   این معادله هم قابل حل با روش عددی یا سری‌هاست.

 

# 7. معادلات قابل تجزیه به دو معادله مرتبه اول

 

## 🔎 روش تشخیص

اگر معادله دیفرانسیل مرتبه دوم را بتوان به‌صورت یک **دستگاه شامل دو معادله دیفرانسیل مرتبه اول** نوشت، آنگاه معادله قابل تجزیه است.

ساختار عمومی:

$$
y'' = f(x, y, y')
$$

با تعریف:

$$
p = y' \Rightarrow \begin{cases}
\frac{dy}{dx} = p \\
\frac{dp}{dx} = f(x, y, p)
\end{cases}
$$

 

## 🛠️ روش حل

1. تعریف متغیر کمکی $p = y'$.
2. نوشتن دستگاه دو معادله مرتبه اول:
   $$
   \frac{dy}{dx} = p, \quad \frac{dp}{dx} = f(x, y, p)
   $$
3. استفاده از روش‌های حل دستگاه معادلات دیفرانسیل مرتبه اول (تحلیلی یا عددی).
4. در صورت امکان، حل معادله دوم برای $p(x)$ و سپس انتگرال‌گیری برای $y(x)$.

 

## 💡 نکات کلیدی و دام‌های رایج

- این روش در واقع **یک تکنیک عمومی** برای حل معادلات مرتبه دوم است.
- اگر نتوان معادله را با روش‌های کلاسیک حل کرد، این تجزیه به دو معادله مرتبه اول ممکن است مسیر حل عددی را هموار کند.
- در حل عددی (مثلاً روش رانگه-کوتا)، این تجزیه بسیار مفید است.
- در برخی مسائل، یکی از معادلات ممکن است بسیار ساده و دیگری پیچیده باشد؛ تمرکز باید روی سخت‌تر باشد.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: تابع سینوسی و لگاریتمی

حل کنید:

$$
y'' + y' \ln(y) = \sin(x)
$$

**حل:**

1. بگذارید:

   $$
   p = y' \Rightarrow y'' = \frac{dp}{dx}
   $$

   معادله:

   $$
   \frac{dp}{dx} + p \ln(y) = \sin(x)
   $$

   و:

   $$
   \frac{dy}{dx} = p
   $$

2. دستگاه معادلات:

   $$
   \begin{cases}
   \frac{dy}{dx} = p \\
   \frac{dp}{dx} = \sin(x) - p \ln(y)
   \end{cases}
   $$

   این دستگاه به‌صورت عددی قابل حل است (مثلاً روش رانگه-کوتا ۴).

 

### 🧪 تمرین ۲: معادله خطی با تابع نمایی

حل کنید:

$$
y'' - 3y' + 2e^y = 0
$$

**حل:**

1. تعریف $p = y'$:

   $$
   y'' = \frac{dp}{dx}
   $$

   پس معادله:

   $$
   \frac{dp}{dx} - 3p + 2e^y = 0
   $$

2. دستگاه معادلات:

   $$
   \begin{cases}
   \frac{dy}{dx} = p \\
   \frac{dp}{dx} = 3p - 2e^y
   \end{cases}
   $$

   این معادله **تحلیلی به‌سختی حل می‌شود**، اما می‌توان به‌صورت عددی یا با تقریب پاسخ را یافت.

 

### 🧪 تمرین ۳: معادله از نوع نوسان غیرخطی

حل کنید:

$$
y'' + y (y')^2 = 0
$$

**حل:**

1. تعریف $p = y'$:

   $$
   y'' = \frac{dp}{dx}
   $$

   پس:

   $$
   \frac{dp}{dx} + y p^2 = 0
   $$

   دستگاه:

   $$
   \begin{cases}
   \frac{dy}{dx} = p \\
   \frac{dp}{dx} = -y p^2
   \end{cases}
   $$

2. معادله به‌صورت عددی قابل حل است.

   یک راه حل تحلیلی: جدا کردن متغیرها:

   $$
   \frac{dp}{dy} = \frac{dp/dx}{dy/dx} = \frac{-y p^2}{p} = -y p
   $$

   پس:

   $$
   \frac{dp}{dy} = -y p \Rightarrow \frac{dp}{p} = -y \, dy
   $$

3. انتگرال‌گیری:

   $$
   \ln|p| = -\frac{y^2}{2} + C_1 \Rightarrow p = y' = C e^{-y^2/2}
   $$

4. انتگرال‌گیری نهایی:

   $$
   \frac{dy}{dx} = C e^{-y^2/2} \Rightarrow \int \frac{dy}{e^{-y^2/2}} = C \int dx
   $$

   که سمت چپ به تابع خطای گاوسی $\text{erf}$ مربوط است.

✅ نتیجه: معادله به‌صورت نیمه‌تحلیلی قابل حل است.

 

# 8. معادلات خطی با ضرایب متغیر

 

## 🔎 روش تشخیص

معادله دیفرانسیل مرتبه دوم از نوع خطی با ضرایب متغیر به‌صورت کلی نوشته می‌شود:

$$
a(x) y'' + b(x) y' + c(x) y = f(x)
$$

که در آن $a(x)$، $b(x)$ و $c(x)$ توابعی از $x$ هستند (و نه ضرایب ثابت).

 

## 🛠️ روش‌های حل

بسته به نوع و ساختار معادله، چندین روش ممکن است کاربرد داشته باشد:

### 1. **روش تغییر متغیر مناسب**
- اگر با تغییر متغیر خاصی، ضرایب تبدیل به ثابت شوند، می‌توان معادله را به حالت با ضرایب ثابت برد.

### 2. **روش کاهش مرتبه**
- اگر یک جواب خاص از معادله همگن را داشته باشیم، می‌توان با فرض $y = u(x) y_1(x)$ معادله را به مرتبه اول کاهش داد.

### 3. **روش سری‌ها (سری توانی)**
- برای نقاط منظم یا نقاط تکین منظم، می‌توان جواب را به‌صورت سری توانی نوشت.

### 4. **روش تغییر پارامتر (Variation of Parameters)**
- برای حل غیرهمگن، در صورت داشتن جواب‌های همگن.

 

## 💡 نکات کلیدی

- اگر بتوان معادله را به فرم استاندارد تبدیل کرد:
  $$
  y'' + P(x) y' + Q(x) y = R(x)
  $$

  آنگاه تحلیل ساده‌تر است.

- بسیاری از معادلات فیزیکی مهم (مانند معادله بسل، لگاند و ...) در این دسته قرار می‌گیرند.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: معادله با ضریب نمایی

حل کنید:

$$
x^2 y'' + x y' - y = 0
$$

**حل:**

1. این معادله دارای ضرایب متغیر است و از نوع **معادله اویلر-کوشی** است.

   بنابراین بگذارید:

   $$
   x = e^t \Rightarrow y(x) = u(t), \quad \text{و استفاده از زنجیره مشتق‌گیری}
   $$

   مشتق‌ها تبدیل می‌شوند به:

   $$
   \frac{dy}{dx} = \frac{du}{dt} \cdot \frac{dt}{dx} = \frac{1}{x} \frac{du}{dt}
   $$

   و با جایگذاری در معادله و ساده‌سازی به معادله با ضرایب ثابت می‌رسیم:

   $$
   u'' - u = 0 \Rightarrow u(t) = A e^t + B e^{-t}
   $$

   پس:

   $$
   y(x) = A x + \frac{B}{x}
   $$

 

### 🧪 تمرین ۲: معادله خاص با ضریب لگاریتمی

حل کنید:

$$
\ln(x) y'' + y = 0 \quad (x > 0)
$$

**حل:**

روش خاصی در دست نیست، اما با فرض جواب به‌صورت سری توانی یا تغییر متغیر:

بگذارید:

$$
t = \ln(x) \Rightarrow x = e^t
$$

تغییر متغیر و اعمال مشتقات پیچیده خواهد بود؛ یا می‌توان از حل عددی استفاده کرد.

 

### 🧪 تمرین ۳: روش سری‌ها – نقطه منظم

حل کنید:

$$
(1 - x^2) y'' - 2x y' + 2y = 0
$$

**حل:**

این معادله، یک معادله معروف لگاند (Legendre) است.

نقطه $x = 0$ یک نقطه منظم است، پس می‌توان جواب را به‌صورت سری توانی فرض کرد:

$$
y(x) = \sum_{n=0}^\infty a_n x^n
$$

و با جایگذاری در معادله و تطبیق توان‌ها، ضرایب بازگشتی پیدا می‌شوند.

این معادله دارای جواب‌های خاص و مهم است و در فیزیک کاربرد فراوان دارد.

 

### 🧪 تمرین ۴: استفاده از تغییر پارامتر

حل کنید:

$$
y'' - \frac{2}{x} y' + \frac{2}{x^2} y = \frac{\sin x}{x^2}
$$

**حل:**

1. معادله همگن:

   $$
   y'' - \frac{2}{x} y' + \frac{2}{x^2} y = 0
   $$

   معادله اویلر-کوشی اصلاح‌شده است. با امتحان جواب $y = x^r$:

   $$
   r(r-1) - 2r + 2 = 0 \Rightarrow r^2 - 3r + 2 = 0 \Rightarrow r = 1, 2
   $$

   جواب همگن:

   $$
   y_h = C_1 x + C_2 x^2
   $$

2. روش تغییر پارامتر:

   با استفاده از فرمول:

   $$
   y_p = u_1(x) y_1(x) + u_2(x) y_2(x)
   $$

   و تعیین $u_1'$ و $u_2'$ با فرمول‌های کلاسیک، $y_p$ محاسبه می‌شود.

   نهایتاً:

   $$
   y(x) = y_h + y_p
   $$

 

# 9. معادلات حل‌پذیر خاص با روش سری‌ها یا تغییر متغیر ویژه

 

## 🔎 روش تشخیص

این دسته شامل معادلاتی است که به روش‌های معمول مانند ضرایب ثابت یا کاهش مرتبه قابل حل نیستند اما:

- نقطه‌ای به عنوان **نقطه عادی یا نقطه تکین منظم** دارند.
- فرم استاندارد معادله به‌شکل:

  $$
  y'' + P(x) y' + Q(x) y = 0
  $$

  است که $P(x)$ و $Q(x)$ توابعی هستند که در یک نقطه خاص (معمولاً $x = 0$) ممکن است رفتار خاصی داشته باشند.

- حل این معادلات با استفاده از **سری توانی** یا **تغییر متغیر مناسب** ممکن است.

 

## 🛠️ روش‌های حل

### 1. **روش سری توانی (Power Series)**

مفید در نقاط عادی یا تکین منظم.

فرض می‌کنیم:

$$
y(x) = \sum_{n=0}^{\infty} a_n (x - x_0)^n
$$

و مشتقات را گرفته و در معادله جای می‌دهیم، سپس با تطبیق توان‌ها، ضرایب بازگشتی $a_n$ را پیدا می‌کنیم.

 

### 2. **روش تغییر متغیر ویژه**

اگر بتوان متغیر $x$ را با یک تابع مناسب مانند $x = e^t$ یا $x = \tan \theta$ تغییر داد تا معادله ساده شود، استفاده می‌شود.

 

## 💡 نکات کلیدی

- نقاط تکین (singular points) به دو نوع تقسیم می‌شوند:
  - **نقطه تکین منظم (regular singular)**: روش فروبنیوس قابل استفاده است.
  - **نقطه تکین نامنظم (irregular singular)**: اغلب نیاز به روش‌های پیشرفته‌تر دارد.

- برخی معادلات کلاسیک مهم:
  - معادله بسل: $x^2 y'' + x y' + (x^2 - \nu^2)y = 0$
  - معادله لگاند: $(1 - x^2)y'' - 2x y' + n(n+1)y = 0$

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: حل با سری توانی در نقطه منظم

حل کنید:

$$
y'' - x y = 0
$$

**حل:**

فرض می‌کنیم:

$$
y(x) = \sum_{n=0}^{\infty} a_n x^n
\Rightarrow
y'' = \sum_{n=2}^{\infty} n(n-1) a_n x^{n-2}
$$

و:

$$
x y = \sum_{n=0}^{\infty} a_n x^{n+1}
$$

در نتیجه:

$$
\sum_{n=2}^{\infty} n(n-1) a_n x^{n-2} - \sum_{n=0}^{\infty} a_n x^{n+1} = 0
$$

با تغییر اندیس و تطبیق توان‌ها رابطه بازگشتی به دست می‌آید. با انتخاب $a_0$ و $a_1$ دلخواه، سری جواب حاصل می‌شود.

 

### 🧪 تمرین ۲: معادله بسل مرتبه صفر

حل کنید:

$$
x^2 y'' + x y' + x^2 y = 0
$$

**حل:**

معادله بسل با $\nu = 0$ است.

فرض جواب به‌صورت سری:

$$
y(x) = \sum_{n=0}^{\infty} a_n x^{n + r}
$$

با جایگذاری در معادله، معادله‌ای بازگشتی برای ضرایب $a_n$ به دست می‌آید.

جواب نهایی:

$$
y(x) = a_0 J_0(x) + a_1 Y_0(x)
$$

که $J_0$ و $Y_0$ توابع بسل مرتبه صفر از نوع اول و دوم هستند.

 

### 🧪 تمرین ۳: تغییر متغیر نمایی

حل کنید:

$$
x^2 y'' + 3x y' + 2y = 0
$$

**حل:**

تغییر متغیر $x = e^t$ را امتحان می‌کنیم. با محاسبه مشتقات و جایگذاری، به معادله با ضرایب ثابت می‌رسیم:

$$
\frac{d^2y}{dt^2} + y = 0
\Rightarrow
y(t) = A \cos t + B \sin t
$$

برگرداندن به متغیر $x$:

$$
t = \ln x \Rightarrow y(x) = A \cos(\ln x) + B \sin(\ln x)
$$

 

# 10. معادلات قابل حل با تغییر متغیر خاص یا تابع ویژه

 

## 🔎 روش تشخیص

این معادلات دارای ساختار خاصی هستند که به معادلات معروفی مانند بسل، لگاند، هایپرژئومتریک و … تقلیل می‌یابند.

- شکل عمومی آن‌ها دارای ضرایب وابسته به $x$ است که نشان‌دهنده نبود ضرایب ثابت است.
- معادله به یکی از معادلات استاندارد تابع ویژه شبیه است:

  - معادله بسل:
    $$
    x^2 y'' + x y' + (x^2 - \nu^2) y = 0
    $$

  - معادله لگاند:
    $$
    (1 - x^2) y'' - 2x y' + n(n+1) y = 0
    $$

  - معادله هایپرژئومتریک گاوس:
    $$
    x(1 - x)y'' + [c - (a + b + 1)x] y' - ab y = 0
    $$

 

## 🛠️ روش‌های حل

### 1. **تشخیص نوع تابع ویژه**

اگر معادله را بتوان به یکی از فرم‌های بالا رساند، پاسخ آن از روی حل شناخته‌شده آن تابع ویژه قابل نوشتن است.

### 2. **تغییر متغیر مناسب**

گاهی با تغییر متغیر هوشمندانه می‌توان معادله را به یک معادله بسل، لگاند یا دیگر فرم‌های استاندارد تبدیل کرد.

### 3. **استفاده از سری فروبنیوس**

در نقاط تکین منظم، روش فروبنیوس ممکن است به یک تابع ویژه منجر شود.

 

## 💡 نکات کلیدی

- این دسته از معادلات در مسائل فیزیکی (مکانیک کوانتومی، ارتعاشات، گرما، الکترومغناطیس) بسیار رایج‌اند.
- نقاط **تکین منظم** معمولاً وجود دارد؛ باید تشخیص داده شود که کدام تابع ویژه قابل اعمال است.
- در بسیاری از موارد، **توابع ویژه به صورت سری توانی** تعریف می‌شوند.
- **دام رایج:** ناتوانی در تشخیص فرم تابع ویژه یا تلاش برای حل عددی بدون توجه به راه‌حل تحلیلی خاص آن.

 

## 🧠 تمرینات دشوار همراه با حل تشریحی

 

### 🧪 تمرین ۱: معادله بسل مرتبه یک

حل کنید:

$$
x^2 y'' + x y' + (x^2 - 1) y = 0
$$

**حل:**

این معادله بسل با $\nu = 1$ است.

جواب عمومی:

$$
y(x) = A J_1(x) + B Y_1(x)
$$

که در آن:

- $J_1(x)$ تابع بسل از نوع اول مرتبه ۱ است.
- $Y_1(x)$ تابع بسل از نوع دوم (نئیمن) مرتبه ۱ است.

 

### 🧪 تمرین ۲: تبدیل به معادله لگاند

حل کنید:

$$
(1 - x^2) y'' - 2x y' + 6y = 0
$$

**حل:**

این معادله لگاند با $n(n+1) = 6 \Rightarrow n = 2$ است.

جواب عمومی:

$$
y(x) = A P_2(x) + B Q_2(x)
$$

که:

- $P_2(x) = \frac{1}{2}(3x^2 - 1)$ چندجمله‌ای لگاند مرتبه ۲
- $Q_2(x)$ تابع لگاند نوع دوم

 

### 🧪 تمرین ۳: تغییر متغیر به معادله بسل

حل کنید:

$$
x^2 y'' + 5x y' + (4 - x^2) y = 0
$$

**حل:**

بیایید تغییر متغیر $x = 2t$ انجام دهیم:

$$
\Rightarrow \quad y'' = \frac{1}{4} \frac{d^2y}{dt^2}, \quad y' = \frac{1}{2} \frac{dy}{dt}
$$

جایگذاری منجر به:

$$
t^2 y'' + 2.5t y' + (1 - t^2)y = 0
$$

تقریباً به فرم بسل با تغییرات اندک نزدیک می‌شود. سپس با تنظیم ضرایب و استفاده از توابع بسل حل می‌شود.

 

### 🧪 تمرین ۴: سری فروبنیوس

حل کنید:

$$
x^2 y'' + x y' + (x^2 - \frac{1}{4}) y = 0
$$

**حل:**

نقطه $x = 0$ تکین منظم است. از سری فروبنیوس استفاده می‌کنیم:

$$
y(x) = x^r \sum_{n=0}^{\infty} a_n x^n
$$

جایگذاری و تعیین $r$ از معادله شاخص و سپس یافتن رابطه بازگشتی $a_n$ به حل کامل می‌انجامد. جواب به شکل:

$$
y(x) = A J_{1/2}(x) + B Y_{1/2}(x)
$$

 









